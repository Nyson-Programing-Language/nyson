dec str number: "-1";
dec str loading: "";
loop(7) {
    loading: "";
    loop(number) {
        loading: loading "#";
    }
    loop(5-number) {
        loading: loading "-";
    }
    log(loading);
    sleep(1000);
    number = math(number+1);
}